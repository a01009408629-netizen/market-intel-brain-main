//! Vector Store subsystem (backed by Qdrant)
//!
//! Stub implementation – replace with real Qdrant client logic as needed.

/// Initialise the vector-store subsystem.
pub fn init() {
    tracing::info!("Vector store subsystem initialised");
}

/// Clean up the vector-store subsystem on shutdown.
pub fn cleanup() {
    tracing::info!("Vector store subsystem cleaned up");
}